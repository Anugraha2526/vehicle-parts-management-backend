namespace vehicle_parts_management_backend.Domain.Enums
{
    // roles that control what a user is allowed to do in the system
    public enum UserRole
    {
        Admin = 1,
        Staff = 2,
        Customer = 3
    }
}
